{
	"name": "SeanBeansLastScenes",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"build-linux": "echo fs.inotify.max_user_watches=582222 | sudo tee -a /etc/sysctl.conf && sudo sysctl -p && react-native start",
    "start-linux": "react-native run-android",
		"test": "jest"
	},
	"dependencies": {
		"axios": "^0.17.1",
		"react": "16.0.0",
		"react-native": "0.50.4",
		"react-native-video": "^2.0.0",
		"react-native-video-controls": "^2.0.0",
		"react-navigation": "^1.0.0-beta.21"
	},
	"devDependencies": {
		"babel-jest": "21.2.0",
		"babel-preset-react-native": "4.0.0",
		"jest": "21.2.1",
		"react-test-renderer": "16.0.0"
	},
	"jest": {
		"preset": "react-native"
	}
}
